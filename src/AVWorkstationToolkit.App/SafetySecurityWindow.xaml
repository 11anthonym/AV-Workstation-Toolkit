<Window x:Class="AVWorkstationToolkit.App.SafetySecurityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Safety &amp; Security - AV Workstation Toolkit" Width="680" Height="560" MinWidth="520" MinHeight="400"
        Icon="/AVWorkstationToolkit.App;component/Assets/AVWorkstationToolkit.ico"
        WindowStartupLocation="CenterOwner" Background="#0A0F1C" Foreground="#E8EEF8"
        FontFamily="Segoe UI" FontSize="13" AutomationProperties.Name="AV Workstation Toolkit safety and security">
    <Grid Margin="22"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock x:Name="SafetyHeading" Text="Safety &amp; Security" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,14"/>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <TextBlock x:Name="SafetyBody" TextWrapping="Wrap" Foreground="#C7D3E3" LineHeight="21" Padding="2"><Run Text="AV Workstation Toolkit runs locally as a standard user. It doesn't provide a web server or accept remote commands."/><LineBreak/><LineBreak/><Run Text="Only supported apps you select can be installed or updated. AVWT checks each app again before it starts. An app's installer may show the normal Windows administrator approval prompt."/><LineBreak/><LineBreak/><Run Text="Vendor downloads are checked against the catalog before AVWT shows them in File Explorer. AVWT doesn't run vendor installers. Device Lookup and information-only entries can't start an installation or download."/><LineBreak/><LineBreak/><Run Text="AVWT doesn't uninstall apps or roll back software changes. A pending Windows restart pauses changes that install a driver, add a background service, or accept network connections. Other low-impact apps may remain available."/><LineBreak/><LineBreak/><Run Text="For SFTP downloads, AVWT confirms the vendor server before it uses your sign-in. Saved passwords stay in Windows Credential Manager and are removed from logs, diagnostics, and action files."/></TextBlock>
        </ScrollViewer>
        <Button Grid.Row="2" Content="Close" IsDefault="True" IsCancel="True" MinWidth="90" HorizontalAlignment="Right" Margin="0,14,0,0" Padding="16,7" Click="Close_Click"/>
    </Grid>
</Window>
